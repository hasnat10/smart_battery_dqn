# smart_battery_dqn

Battery control project using a DQN agent.

## How to run
1) Create and activate venv
python -m venv .venv
source .venv/bin/activate

2) Install dependencies
pip install -r requirements.txt

3) Run
python main.py

## Outputs
- results_*.csv
- plot_*.png
